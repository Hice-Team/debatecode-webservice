diff --git a/node_modules/@opennextjs/aws/dist/build/copyTracedFiles.js b/node_modules/@opennextjs/aws/dist/build/copyTracedFiles.js
index 9701b04..c44ae5a 100644
--- a/node_modules/@opennextjs/aws/dist/build/copyTracedFiles.js
+++ b/node_modules/@opennextjs/aws/dist/build/copyTracedFiles.js
@@ -228,7 +228,13 @@ File ${serverPath} does not exist
                 symlinkSync(symlink, to);
             }
             catch (e) {
-                if (e.code !== "EEXIST") {
+                if (e.code === "EPERM") {
+                    // Windows without symlink privilege (no Developer Mode/admin):
+                    // copy the real target instead of recreating the symlink.
+                    const realTarget = path.resolve(path.dirname(from), symlink);
+                    cpSync(realTarget, to, { recursive: true });
+                }
+                else if (e.code !== "EEXIST") {
                     throw e;
                 }
             }
