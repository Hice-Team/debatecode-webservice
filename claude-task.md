# 작업 체크리스트

> 진행 상황 추적용. 완료 시 `[x]`, 진행 중 `[~]`, 보류/확인필요 `[?]`.

## ⚠️ 직접 하셔야 하는 일 (코드로 해결 불가)

| # | 할 일 | 관련 작업 |
|---|---|---|
| 1 | **Google Cloud 콘솔 설정** — 프로젝트에서 Picker API 사용 설정, OAuth 2.0 클라이언트 ID 발급(승인된 JS 원본에 서비스 도메인 등록), API 키 발급, 프로젝트 번호 확인. 이후 `.env`의 `NEXT_PUBLIC_GOOGLE_CLIENT_ID` · `NEXT_PUBLIC_GOOGLE_API_KEY` · `NEXT_PUBLIC_GOOGLE_APP_ID` 세 값을 채우고, 배포용은 GitHub Secrets에도 등록 | AI 검색 1번 |
| 2 | **DB 마이그레이션 실행** — `npx prisma db execute --file prisma/manual-additive.sql --schema prisma/schema.prisma` (AiFeedback 표 생성). 배포 DB에도 동일하게 1회 | AI 검색 2번 |
| 3 | **사업자 등록** (1인팀) — 등록 후 번호·상호·주소를 알려주시면 푸터·약관에 반영 | 푸터/약관 |
| ~~4~~ | ~~더치트 제휴/API~~ — **결정 완료: 딥링크**. API로 붙이면 거래 상대의 계좌·연락처가 우리 서버를 거쳐 제3자에게 가서 개인정보 처리 위탁 고지 대상이 늘어납니다. 딥링크는 같은 효과에 그 부담이 없습니다 | 커뮤니티 |
| 5 | **(선택) GITHUB_TOKEN 발급** — 없어도 동작하지만 시간당 60회 제한. 발급 시 5,000회 | AI 검색 8번 |
| ~~6~~ | ~~콘솔에서 시즌 1 + 랭킹 초기화 실행~~ — **완료(2026-08-20)** — 관리 콘솔 › 시즌·랭킹 › [시즌 1로 초기화] → [전체 랭킹 초기화]. 코드로 미리 눌러 둘 수 없는 운영 판단이라 남겨 둡니다 | 커뮤니티 1번 |
| ~~7~~ | ~~첨부 스토리지 공개 여부~~ — **완료**. AI 첨부는 비공개 버킷 `ai-attachments` + 5분 서명 URL로 분리. 커뮤니티 첨부는 공개 글에 붙는 것이라 그대로 둠 | 보안 점검 |

## AI 검색 섹션
- [x] 구글 Drive API 연동 및 파일 추가 시스템 검토 — 권한 범위 drive.file 전환, 스크립트 로드 실패 캐시 버그, 부분 실패 안내 추가. **미결: 첨부 버킷이 public**
- [x] 답변 피드백 시스템 — AiFeedback 테이블·사유 카탈로그·사유 패널 UI·관리콘솔 `/console/ai-feedback`
- [x] 사고 과정 > 답변 전환 부드럽게 — 중복 렌더 깜빡임 제거, 단계 목록 접기 애니메이션, `animate-in` 유틸리티 실제 구현
- [x] TTS 변경 — 공용 `useSpeech` 훅(문장 분할·크롬 15초 끊김/절전 대응·속도·일시정지), 재생 바 UI, 3개 화면 통합
- [x] 브랜치 작성 UI/UX (ChatGPT 레퍼런스) — 분기 지점까지 대화 **전체** 복사, 분기 배너 + 원본 링크, 진행 표시
- [x] 파일 업로드 썸네일 카드 UI 개선 (Google 레퍼런스) — 카드 전체 클릭 → 미리보기, 호버 그림자
- [x] 파일 미리보기 (Google 레퍼런스) — 전체화면 뷰어(이미지·영상·오디오·PDF·텍스트), ←/→ 넘기기, Esc 닫기
- [x] GitHub 코드 가져오기 — 저장소 주소 붙여넣으면 코드·문서 최대 40개 자동 선별(트리 API + raw), 파일 주소는 기존대로

## 코드에디터 / 문제 풀이 화면
- [x] 신고 팝업 UI 수정 — Esc 닫기·초점 복귀·배경 스크롤 잠금, 사유 선택 표시, 버튼줄 고정, 100dvh 대응
- [x] 코드에디터에서 전역 배너 숨기기 — `BannerSlot`이 `/problems/{숫자}`에서 배너를 제외
- [x] debateAI 탭 실시간 생성 — NDJSON 스트리밍 전환, 커서 표시, 중단해도 받은 내용 보존

## 커뮤니티
- [x] 시즌 1로 설정 + 랭킹 초기화 — **DB에 적용 완료**. 현재 S1(2026-08-20 KST 시작), 랭킹은 그 시각부터 집계
- [x] 관리콘솔 시즌·랭킹 — `/console/seasons` 신설. 시즌 번호 변경/초기화, 전체 랭킹 초기화(+해제), 계정별 랭킹 초기화(+취소), 전부 감사 로그 기록. `ranking.manage` 권한 신설
- [x] 공지게시판 관리자 전용 + 공지 체크 시 모든 게시판 상단 고정 — `Post.pinned`, 쓸 수 없는 게시판은 선택지에서 제외
- [x] 공지 고정 토글 — 글 화면 압정 버튼 하나로 켜고 끔(감사 로그 기록). `announcement.manage` 권한
- [x] 문의게시판 개편 — 답글은 관리자·메이트·협력사만(비밀글은 관리자만), 채택 포인트 10~50P 질문자 지정, 비밀글 선택제(기존 강제 비밀글 해제), 익명은 기존 기능 유지. 채택 포인트는 **메이트만** 적립
- [~] 중고 게시판 — **데이터·정책·액션 계층 완료**: `MarketListing`/`MarketChat`/`MarketMessage` 3표, 상태 3종(판매중·예약중·판매완료), 상품등급 4종, 택배/직거래, 더치트 딥링크, 이메일 인증 게이트, 1:1 대화(각자 숨김·수정·소프트 삭제·읽음), 상태 변경 시 대화방에 시스템 메시지. **남은 것: 화면(등록 폼·매물 카드·채팅 UI·대시보드)**

## 회원가입
- [ ] 가입 플로우 점검
- [ ] 이메일 인증 필수 → 선택으로 변경, 가입 중 API 키 입력·2차 보안 설정
- [x] 이메일 인증자만 중고 거래 허용 — `canTrade()` 정책 + `auth.users.email_confirmed_at` 확인. 글쓰기·채팅 시작 양쪽에 적용

## 프로필 메뉴
- [ ] 디베이트메이트 클릭 시 권한 여부에 따라 메이트 페이지 / 메이트 콘솔로 분기
- [ ] 커뮤니티 프로필 메뉴 추가 (프로필 관리 / 게시글·답글 관리 / 중고거래 관리 / 문의 관리)

## 설정 페이지
- [x] 접속 불가 문제 해결 — 원인은 **인증됐지만 앱 계정 행이 없는 반쪽 상태**의 무한 리다이렉트/크래시. `/auth/recover` 탈출구 + 전역 오류 경계 신설
- [ ] 전체 앱에 필요한 설정 항목 파악 후 추가

## 푸터 및 약관
- [x] **디베이트포인트 약관 분리 신설** — `/legal/point-terms`. 포인트 성격·문제풀이 산정식·기여활동 지급표·문의 채택 포인트·**메이트 추가 지급 정책**·사용/환불·부정 적립 회수·변경 고지. 금액은 코드 상수에서 직접 읽어 약관과 실제 정책이 어긋나지 않게 함. 푸터·설정·사이트맵 등록, 메이트 약관의 중복 조항은 링크로 정리
- [ ] 제작사 정보: HICE Team (Luce 402 Studio 개발)
- [ ] 자체 푸터 제작
- [ ] 약관 갱신 (한국 법령 기준)
- [ ] 사업자 등록 예정(1인팀) 반영
- [ ] 사이트맵 / 뉴스피드 시스템
- [ ] debatecode.hicecorp.com 접속 시 안내 배너 (비즈니스용 도메인 — 둘러보기 전용)
- [ ] 기기 탐지 및 반응형 디자인

## 마무리
- [ ] 유지보수 가이드북 작성
- [ ] GitHub 커밋 및 푸시
- [ ] 버그 수정 및 ESLint 정리
- [ ] 보안 점검 및 사용 플로우 QA

## 디자인 시스템 (oh-my-design + hallmark)
- [x] `omd:init` — 레퍼런스 **linear.app** 채택, 루트 `DESIGN.md` 부트스트랩(15개 섹션), `.omd/init-context.json`, shim 3종(CLAUDE.md·AGENTS.md·.cursor) 설치
- [x] 명도 계단 도입 — 임의 불투명도 회색 13단계를 `fg / fg-secondary / fg-muted / fg-quiet` 4단계로. **1,638곳 / 172파일** 이관
- [x] 접근성 개선 — `/55 /50 /45`(366곳)가 AA 미달(3.9~2.9:1)이었는데 `fg-muted` 4.5:1로 통과. `fg-quiet`도 2.4→3.1:1로 상향
- [x] 반경 정리 — 6종 → `control 6 / card 8 / panel 12 / pill` (2xl·3xl 163곳 접기)
- [x] 모션 통일 — `transition-all` 17곳 제거, easing/duration 토큰화
- [x] 한글 조판 — 제목 `word-break: keep-all`, 이탤릭 제목 차단, 숫자 `tabular-nums`
- [~] 화면별 hallmark redesign — 랜딩 완료(섹션 리듬 3종 분리, 아이브로우 6곳 → 번호 표식, 통계 tabular-nums). 커뮤니티·문제풀이·대시보드 남음
- [ ] hallmark 최종 slop audit + 320/375/414/768px 반응형 검증

## 공통
- [ ] 팝업이 헤더에 가려지거나 화면 밖으로 넘어가지 않도록 개선
- [ ] 페이지 전환 애니메이션 / 로딩 화면
- [ ] 전반적 성능 최적화 + ESLint 문제 해결
- [ ] 불필요한 파일 제거 및 파일 구조 정리
