---
name: debateCode
bootstrapped_from: linear.app
mode: inspired
omd: "0.1"
bootstrapped_at: "2026-08-20"
maker: HICE Team (Luce 402 Studio)
locale: ko-KR
tokens:
  colors:
    # 크롬 — 네비게이션 · 코드 에디터 · 히어로. Linear의 dark-native 캔버스 계열.
    ink: "#08091a"
    ink-soft: "#12142c"
    # 콘텐츠 표면 — 대시보드 · 커뮤니티 · 설정. Linear에는 없는 debateCode 고유 층.
    paper: "#f5f6fb"
    surface: "#ffffff"
    # 정체성 인디고. Linear의 #5e6ad2와 같은 역할이되 워드마크 "Code"에서 뽑은 우리 값.
    brand: "#1800AC"
    signal: "#4531d9"
    signal-soft: "#5b4cf0"
    # 명도 계단 — 아래 §2. 임의 opacity 대신 이 네 단계만 쓴다.
    fg: "#08091a"
    fg-secondary: "#3b3f57"
    fg-muted: "#6b7086"
    fg-quiet: "#868ca2"
    # 다크 표면에서의 같은 계단
    fg-on-dark: "#f7f8fa"
    fg-on-dark-secondary: "#c8ccd8"
    fg-on-dark-muted: "#8a8f9e"
    fg-on-dark-quiet: "#6e727a"
    hairline: "rgba(8, 9, 26, 0.08)"
    hairline-on-dark: "rgba(255, 255, 255, 0.08)"
  typography:
    display: { family: display, size: "40px", weight: 700, lineHeight: "1.08", tracking: "-0.022em" }
    heading: { family: display, size: "24px", weight: 700, lineHeight: "1.3", tracking: "-0.012em" }
    body: { family: sans, size: "15px", weight: 400, lineHeight: "1.7", tracking: "-0.005em" }
    navigation: { family: sans, size: "13px", weight: 500, lineHeight: "1.5", tracking: "-0.008em" }
    mono: { family: mono, size: "13px", weight: 400, lineHeight: "1.6", tracking: "0" }
  rounded:
    control: "6px"
    card: "8px"
    panel: "12px"
    pill: "9999px"
  density: dense
---

# debateCode Design System

> 부트스트랩 출처: [linear.app](https://linear.app) (omd:init, 2026-08-20). Linear의
> **톤&매너**(명도 우선 위계 · 절제된 정체색 · 도구다운 정밀함)를 보존하고, debateCode의
> 맥락(한국어 학습 서비스 · 이중 캔버스 · 학습자 밀도)만 델타로 반영했다.
> Linear의 사실 정보(브랜드 서사·페르소나)는 그대로 옮기지 않았다.

## 0. 한 줄

**알고리즘을 푸는 도구답게 정밀하고, 배우는 사람이 지치지 않게 조용하다.**

## 1. Visual Theme & Atmosphere

debateCode는 두 개의 캔버스를 가진다. 이것이 Linear와의 가장 큰 구조적 차이다.

| 층 | 표면 | 쓰는 곳 |
|---|---|---|
| **크롬 / 작업** | `ink` `#08091a` | 네비게이션, 푸터, 코드 에디터 워크스페이스, 히어로, 마케팅 밴드 |
| **콘텐츠** | `paper` `#f5f6fb` + `surface` 흰색 카드 | 대시보드, 커뮤니티, 문제 목록, 설정, 콘솔, 약관 |

Linear는 단일 다크 캔버스지만 debateCode는 **오래 읽고 오래 쓰는 서비스**다. 문제 지문과
커뮤니티 글을 다크에서 읽히게 하면 눈이 먼저 지친다. 그래서 "일하는 화면은 어둡게,
읽는 화면은 밝게"로 나눈다. 페이지는 자신의 팔레트를 **명시적으로 소유**하며
`dark:` 변형은 쓰지 않는다.

두 층 모두에서 위계는 **테두리가 아니라 명도**로 만든다. 이것이 Linear에서 가져온 가장
중요한 규칙이다.

## 2. Color Palette & Roles

### 명도 계단 (가장 중요)

색을 고를 때 `text-ink-soft/45` 같은 임의 불투명도를 쓰지 않는다. 네 단계만 쓴다.

| 역할 | 라이트 표면 | 다크 표면 | 쓰는 곳 |
|---|---|---|---|
| `fg` | `#08091a` | `#f7f8fa` | 제목, 본문 핵심, 지금 읽어야 할 것 |
| `fg-secondary` | `#3b3f57` | `#c8ccd8` | 본문, 설명문 |
| `fg-muted` | `#6b7086` | `#8a8f9e` | 메타데이터, 라벨, 보조 정보 |
| `fg-quiet` | `#868ca2` | `#6e727a` | 비활성, placeholder, 타임스탬프 |

**왜 규칙이 필요한가.** 임의 불투명도는 화면마다 조금씩 다른 회색을 만들고, 그 차이가
쌓이면 "무엇이 더 중요한지"를 색이 더 이상 말해 주지 않는다. 네 단계로 묶으면 같은 종류의
정보가 어느 화면에서나 같은 무게를 갖는다.

### 정체색

- **brand** `#1800AC` — 워드마크 정확값. 로고·브랜드 표기에만.
- **signal** `#4531d9` — 주요 액션(CTA), 선택 상태, 링크.

Linear는 정체색 인디고를 CTA에 쓰지 않고 라이트 스틸을 쓴다. debateCode는 **여기서
의도적으로 갈라진다** — 학습 서비스는 "다음에 무엇을 누를지"가 한눈에 보여야 하고, 중립색
CTA는 그 신호를 약하게 만든다. 대신 Linear의 절제 원칙은 지킨다:

- 한 화면에 signal로 칠한 면(filled)은 **최대 하나**. 나머지 액션은 테두리나 글자만.
- signal을 장식으로 쓰지 않는다. 강조하고 싶으면 명도 계단을 먼저 쓴다.

### 의미색

| 역할 | 색 | 쓰는 곳 |
|---|---|---|
| 성공 / 통과 | `emerald-600` | 테스트 통과, 채택됨, 승인 |
| 주의 | `amber-500` | 공지 고정, 부분 통과, 심사 대기 |
| 위험 / 실패 | `rose-600` | 신고, 삭제, 실패한 테스트 |

의미색은 **상태를 말할 때만** 쓴다. 강조나 장식으로 쓰지 않는다.

## 3. Typography

| 역할 | 패밀리 | 크기 | 굵기 | 행간 | 자간 |
|---|---|---:|---:|---:|---:|
| Display | Space Grotesk | 40px | 700 | 1.08 | -0.022em |
| Heading | Space Grotesk | 24px | 700 | 1.3 | -0.012em |
| Body | Pretendard | 15px | 400 | 1.7 | -0.005em |
| Navigation | Pretendard | 13px | 500 | 1.5 | -0.008em |
| Mono | IBM Plex Mono | 13px | 400 | 1.6 | 0 |

**음수 자간은 큰 글자에만.** Linear가 display에 -1.056px(≈-0.022em)를 주는 이유는 48px에서
글자 사이가 벌어져 보이기 때문이다. 15px 본문에 같은 비율을 주면 한글이 뭉친다 —
본문은 -0.005em까지만.

**한글 규칙 (Linear에 없는 델타)**
- 제목에서 줄을 바꿀 때 단어 중간이 끊기지 않게 `word-break: keep-all`.
- 이탤릭 제목 금지. 강조는 굵기·색으로 한다.
- 숫자와 라틴 문자가 섞이는 자리(포인트·순위·시간)는 mono로 고정해 자릿수가 흔들리지 않게.

## 4. Radius & Shape

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `control` | 6px | 에디터 안 컨트롤, 배지, 작은 태그 |
| `card` | 8px | 목록 행, 메뉴 항목, 입력창 |
| `panel` | 12px | 카드, 다이얼로그, 패널 |
| `pill` | full | 주요 액션 버튼, 필터 칩, 아바타 |

세 단계를 넘기지 않는다. `rounded-lg`/`xl`/`2xl`/`3xl`을 화면마다 다르게 고르는 것이
지금 코드베이스의 가장 흔한 잡음이다.

## 5. Layout Principles

- **테두리보다 명도.** 구획이 필요하면 배경 명도를 먼저 바꾸고, 그래도 모호할 때만 hairline.
- **한 화면 한 초점.** 주요 액션 하나, 그 외는 물러난다.
- **밀도는 목록에, 여백은 읽는 글에.** 문제·게시글 목록은 촘촘하게(행 높이 44–52px),
  지문과 본문은 넉넉하게(행간 1.7, 본문 폭 65–75자).
- **카드를 쌓지 않는다.** 목록은 카드 격자가 아니라 줄(row)이다. 카드는 성격이 다른
  것을 나란히 놓을 때만.

## 6. Depth & Elevation

깊이는 낮고 겹겹이다. 큰 그림자를 쓰지 않는다.

- 카드: `0 1px 3px rgba(8,9,26,0.05)` — 종이 한 장 들린 정도
- 떠 있는 것(메뉴·다이얼로그): `0 12px 32px rgba(8,9,26,0.12)` + hairline
- 다크 표면: 그림자 대신 `rgba(255,255,255,0.06)` 내부 링

## 7. Motion

- 상태 전환 150ms, 진입/퇴장 200ms, 레이아웃 변화 300ms. 그 이상은 느리게 느껴진다.
- easing은 `cubic-bezier(0.16, 1, 0.3, 1)` 하나로 통일.
- `prefers-reduced-motion`에서는 위치 이동을 없애고 페이드만 남긴다. **예외 없음.**
- 장식 애니메이션 금지. 움직임은 "무언가 바뀌었다"를 알릴 때만.

## 8. Do's and Don'ts

### Do
- 명도 계단 네 단계로 위계를 만든다.
- 한 화면에 채워진 signal 버튼 하나.
- 목록은 줄로, 반경은 세 단계로.
- 상태(로딩·빈 목록·오류)를 화면마다 실제로 그린다.

### Don't
- 그라디언트 히어로, 이모지 아이콘, 보라-분홍 그라디언트 텍스트.
- 임의 불투명도(`/45`, `/55`, `/65`…)로 회색 만들기.
- 지어낸 숫자·후기·로고("이용자 10,000명" 같은 것). 실제 값이 없으면 그 자리를 비운다.
- 가짜 브라우저 창·폰 목업·가짜 코드 창 프레임.
- 이탤릭 제목.

## 9. Agent Prompt Guide

> 한국어 알고리즘 학습 서비스를 만든다. 작업 화면(에디터·네비)은 `#08091a` 다크 캔버스,
> 읽는 화면은 `#f5f6fb` 페이퍼에 흰 카드. 위계는 테두리가 아니라 네 단계 명도로 만든다.
> 주요 액션만 인디고 `#4531d9`로 채우고 한 화면에 하나. 반경은 6/8/12px과 pill 넷.
> 제목은 Space Grotesk에 음수 자간, 본문은 Pretendard 15px/1.7. 목록은 카드가 아니라 줄.
> 지어낸 지표·후기를 쓰지 않는다.

## 10. Voice & Tone

한국어 존댓말. 짧고 구체적으로.

- **동작을 말한다.** "제출" "다시 풀기" "채택하기" — "확인" "완료" 같은 빈 라벨 대신.
- **결과를 먼저.** "3개 중 2개 통과" 다음에 이유.
- **사과보다 다음 행동.** "문제가 발생했습니다" 대신 "테스트를 실행하지 못했습니다.
  잠시 후 다시 시도해 주세요."
- **과장하지 않는다.** "완벽한", "혁신적인", "단 한 번에" 금지.
- 이모지는 UI 라벨에 쓰지 않는다(공지 📌처럼 이미 굳어진 자리만 예외).

## 11. Brand Narrative

debateCode는 **HICE Team(Luce 402 Studio)** 이 만드는 알고리즘 학습 서비스다. 코드를 짜는
화면, 스스로 방어해 보는 면접, 사람들이 서로 답을 주고받는 커뮤니티가 한곳에 있다.

핵심 전제는 "정답 코드를 받는 것과 이해하는 것은 다르다"이다. 그래서 AI는 답을 대신 써
주는 자리가 아니라 **왜 그렇게 짰는지 되묻는 자리**에 놓인다(리팩토링모드, 면접 방어).
인터페이스도 같은 태도를 가진다 — 화려하게 설득하지 않고, 지금 무엇을 해야 하는지만
분명히 보여 준다.

> 설립 시점·이용자 수·성과 지표는 **아직 확정되지 않았다.** 화면 어디에도 지어낸 숫자를
> 쓰지 않는다. 사업자 등록이 완료되면 푸터·약관에 실제 정보를 채운다.

## 12. Principles

1. **이해가 통과보다 먼저다.** 정답을 빨리 주는 UI보다, 왜 틀렸는지 보이는 UI를 만든다.
2. **위계는 명도로.** 색과 테두리를 더하기 전에 밝기 차이로 해결한다.
3. **밀도는 목적에 맞게.** 목록은 촘촘하게, 읽는 글은 넉넉하게. 한 값으로 통일하지 않는다.
4. **상태를 감추지 않는다.** 로딩·빈 목록·실패는 실제로 그린다. 조용히 사라지는 화면이 가장 나쁘다.
5. **지어내지 않는다.** 없는 숫자, 없는 후기, 없는 로고를 그리지 않는다.

## 13. Personas

코드베이스에 실재하는 역할에서만 뽑았다(app/lib/permissions.ts).

- **학습자(user)** — 문제를 풀고, 막히면 debateAI에 묻고, 커뮤니티에 질문한다. 화면에서
  가장 자주 하는 일은 "다음에 뭘 풀지 고르기"와 "왜 틀렸는지 보기"다.
- **디베이트메이트(debate_mate)** — 문제를 출제하고 문의에 답한다. 채택되면 포인트를 받는다.
  목록·심사 화면에서 밀도가 중요하다.
- **운영진(admin/reviewer)** — 신고·문의·출제 검토 큐를 처리한다. 콘솔은 예쁨보다
  "얼마나 밀렸는지"가 먼저 보여야 한다.
- **협력사(partner)** — 제휴 범위 안에서 문의에 답한다.

> 이용자 수, 연령대, 사용 시간대 등은 **미확정**. 리서치 전까지 이 문서에 채우지 않는다.

## 14. States

모든 목록·비동기 화면은 네 상태를 갖는다. 하나라도 빠지면 미완성으로 본다.

| 상태 | 규칙 |
|---|---|
| 로딩 | 스피너 단독 금지. 들어올 모양과 같은 골격(skeleton) 또는 진행 단계 |
| 빈 목록 | "왜 비었는지" + "다음에 할 일" 링크. 그림만 있는 빈 화면 금지 |
| 오류 | 무엇이 실패했는지 + 다시 시도 버튼. 원문 스택 노출 금지 |
| 부분 실패 | 조용히 넘기지 않는다. "3개 중 1개를 가져오지 못했습니다" |

## 15. Accessibility (한국어 서비스 기준)

- 본문 대비 4.5:1, 큰 글자 3:1 이상. 실측값(paper `#f5f6fb` 기준):
  `fg` 18.3:1 · `fg-secondary` 9.6:1 · `fg-muted` 4.5:1 · `fg-quiet` 3.1:1.
  `fg-quiet`는 4.5:1에 못 미치므로 **본문에 쓰지 않는다** — 타임스탬프·비활성 라벨 전용.
- 모든 인터랙티브 요소에 보이는 focus 링(`outline: 2px solid signal; offset: 2px`).
- 아이콘만 있는 버튼에는 반드시 `aria-label`.
- 팝업·다이얼로그: Esc로 닫히고, 초점이 안으로 들어갔다가 원래 자리로 돌아온다.
- 터치 대상 최소 44×44px.
- 320 / 375 / 414 / 768px에서 가로 스크롤이 없어야 한다.

---

**Delta from linear.app** — 보존한 것: 명도 우선 위계, 절제된 정체색, 도구다운 정밀함,
음수 자간 display, 6/8px 컨트롤 기하, 상태를 근거 있게 다루는 태도.
바꾼 것: 단일 다크 → **이중 캔버스**(읽는 화면은 페이퍼), 중립 CTA → **인디고 CTA**(학습
서비스의 행동 유도), Inter/Berkeley → **Pretendard/Space Grotesk/IBM Plex Mono**(한글),
밀도 +1(학습 목록), 한글 조판 규칙 추가.
