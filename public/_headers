# Next.js가 해시를 붙여 내보내는 정적 자산 — 내용이 바뀌면 파일명이 바뀐다.
# 그래서 1년 캐시 + immutable로 못 박아도 안전하고, 재방문에서 네트워크 요청이 사라진다.
# (OpenNext 공식 get-started 권장 설정)
/_next/static/*
  Cache-Control: public,max-age=31536000,immutable
